{
  "name": "appex-web",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "admin",
    "packages/lesson-schema"
  ],
  "scripts": {
    "prepare": "npm run build --workspace=@appex/lesson-schema",
    "dev": "concurrently --names frontend,backend,admin --prefix-colors cyan,yellow,magenta \"npm run dev --workspace=frontend\" \"npm run dev --workspace=backend\" \"npm run dev --workspace=admin\"",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:admin": "npm run dev --workspace=admin",
    "dev:admin-stack": "concurrently --names backend,admin --prefix-colors yellow,magenta \"npm run dev --workspace=backend\" \"npm run dev --workspace=admin\"",
    "build": "npm run build --workspace=frontend",
    "build:backend": "npm run build --workspace=backend",
    "build:admin": "npm run build --workspace=admin",
    "lint": "npm run lint --workspace=frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
